<h1>All posts</h1>
<% @post.each do |post|  %>
  <div class="alert alert-light">
  <h3><%=post.title%></h3>
  <p><%=post.body%>
    <%=post.all_tags %>
    <br>
    <%if person_signed_in?%>
    <%=link_to "open",post_path(post),:class => 'btn btn-outline-primary'%>
    <%end %>
  </div>
<% end %>